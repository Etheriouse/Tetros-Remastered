#include "Game.hpp"

Game::Game()
{
}

void Game::render(long double delta, uint64_t tick)
{
    unused(delta);
    unused(tick);
}

void Game::process(long double delta, uint64_t tick)
{
    unused(delta);
    unused(tick);
}